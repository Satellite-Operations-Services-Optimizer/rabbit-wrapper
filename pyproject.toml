[project]
name = "rabbit-wrapper"
version = "0.2"
dependencies = [
    "pika",
    "msgpack",
    "fastapi"
]

[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"