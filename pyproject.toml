[project]
name = "rabbit_wrapper"
version = "0.1"
dependencies = [
    "pika",
    "msgpack",
    "fastapi"
]

[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"